
线程的清理
cleanup.c

线程的取消 cancel

线程竞争实例
    1. primer0.c
    2. primer0e.c
        1中传地址，导致筛质数时，多个线程使用的同一个值；解决方案 1 传值；2、传结构体，每个结构体malloc分配一块空间。

    3. 20个线程读取文件中的数据，并加一放回，可以参考 io/adv/addlock.c


互斥量
    1. 利用互斥量实现一个锁链功能依次打印abcdabcd
    问题：不同进程中能否解锁其余进程的互斥量是未定义行为
        abcd1.c

    2. 线程池的实现
        primer1_pool_busy.c 盲等版本，在等待线程池数据被调用时是盲等，要出让调度器 sched_yield()
        优化后使用 条件变量 ————pthread_cond_wait

条件变量 --》将互斥量时盲等的状态改为通知改变
    1. abcd2.c 根据全局变量值打印abcd，全局变量值改变后通知其余线程。
    2. 线程池优化，盲等num改变 改成 改变后通知。


例子：线程令牌桶的实现 --》互斥量 条件变量 动态模块的单次初始化 线程的取消。。。


信号量：
    1. 自己实现的信号量 sem


线程重入：
    libunlock.c  可以看出正常库函数都支持重入，支持多线程并发；如输入输出时都加锁按序输出。