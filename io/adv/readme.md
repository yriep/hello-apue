非阻塞IO
    1. relay.c  使用状态机实现数据中继
    2. 中继引擎的实例
        1. 头文件中获取的中继引擎job结构体和c文件中的可以不一样，隐藏真实结构，返回出来的都是给别人看的结构。
        2. 类似文件描述符数组，返回数组下标

多路IO：使用状态机时的缺陷
    select_relay
    poll_relay
    epoll_relay

其他的读写函数
    writev..

内存映射 mmap
    1. file_mmap.c
        将文件映射到进程内存空间中，可以像读取字符串一样读取文件，获取文件中字符数量
    2. mem_mmap.c
        使用mmap 动态分配内存空间,可以用作父子间进程的通信。

文件锁
    addlock.c
        20个进程，读取同一个文件中的数字并加一；如果不加锁的结果是什么？加锁 lockf后的结果是什么？

--> 2026.08.09
